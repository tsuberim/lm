[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "lm"
version = "0.1.0"
requires-python = ">=3.9"

[tool.maturin]
python-source = "."
module-name = "lm._tokenizer"
